#include "main.h"
/**
 * swap_int - swap the values of two integers
 * @a: first integer
 * @b: second integer
 * Return: 0 Always
 */

void swap_int(int *a, int *b)
{
	int *a;
	int *b;

	printf("a = %d, b = %d\n", a, b);

	void swap_int(int *a , int *b);

	printf("a = %d, b = %d\n", a, b);
	return (0);
}
